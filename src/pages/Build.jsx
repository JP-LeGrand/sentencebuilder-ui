import React from "react";
import { Grid, Typography } from "@material-ui/core";

export const Build = () => {
  return (
    <Grid container>
      <Typography>Build Page</Typography>
    </Grid>
  );
};
